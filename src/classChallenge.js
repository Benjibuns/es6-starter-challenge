export default function() {
    return "<div>classChallenge</div>"
}

export const greeting = "welcome"

export function arrayTotal(...numList) {
    return numList.reduce((total, num) => total + num)
}